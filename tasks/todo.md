# Todo

- [x] Add a homepage, ask the user which view they want to use (calculator or canvas)
- [x] Add a seperator (dot) after the calculator and canvas buttons
- Make sure export/import buttons work with canvas
- Add a way to view added blocks to the canvas in case it's out of view
- Make sure the tooltip does not go out of the screen when user hovers a resource in canvas (far left resource)
- Move the "how to use this" into a dialog, and have it as a "help" button in the header